import _ from 'lodash';

export default class Observer {
    constructor() {

    }

    define(name, type, customValidation) {

    }

    extendUpdate(name, cb = false) {
        if(!cb && _.isFunction(name)) {

        }
    }

}